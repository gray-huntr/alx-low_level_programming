#include "main.h"
/**
 * add - add two integers and return the result
 * @a: first parameter
 * @b: second parameter
 * Return: a+b
 */
int add(int a, int b)
{
	int output = 0;

	output = a + b;
	return (output);
}
