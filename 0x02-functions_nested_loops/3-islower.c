#include "main.h"
/**
 * _islower - checks if c is lower
 * @c: param to be checked
 * Return: 1 if lowercase
 */
int _islower(int c)
{
	if (c > 98 && c < 123)
		_putchar(r '0' + 1);
	else
		_putchar(r '0' + 0);
}
